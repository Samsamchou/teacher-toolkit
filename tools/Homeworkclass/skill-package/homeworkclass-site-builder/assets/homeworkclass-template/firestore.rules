rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Safe template default. The clone pipeline must render and review
    // firestore.rules.template before any Firebase Rules deployment.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
