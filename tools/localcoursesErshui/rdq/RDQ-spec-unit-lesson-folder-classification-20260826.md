---
rdq_version: 1
edition: chatgpt-app
task: 正式單元依節次建立資料夾
domain: lesson
date: 2026-08-26
status: confirmed
telemetry:
  mode: full
  rounds: 1
  questions: 4
  q4_adopted: 5
  revisions: 0
downstream: ershui-local-curriculum-builder
---

# RDQ 需求規格：正式單元依節次分類

## 一句話任務
把每個正式單元依正式節數建立等量的節次資料夾，先重整〈扇形車庫〉第1、2節，並把規則納入二水在地課程擴編 Skill。

## 已確認
- 每個正式單元有幾節課，單元根目錄就建立幾個依序編號的資料夾：**`01_第1節/`**、**`02_第2節/`**……；此規則適用現有四上及未來四下、三上、三下課程擴編。
- 單元根目錄只保留真正跨節共用的 **`00_工作索引.md`**、來源追溯與設計決定、全單元節次骨架，以及不能合理歸屬單一節次的共用教材。
- 各節正式教案與教材放在該節資料夾根目錄；工作檔、QA證據及歷史版本分放 **`_work/`**、**`_qa/`**、**`_history/`**，並建立該節工作索引。
- 立即重整 **`扇形車庫 第1-2週`**：第1節與第2節所有既有資料分開存入兩個節次資料夾；其他正式單元則先查正式來源確認節數，再建立完整節次結構，尚未開始者放簡短 **`README.md`**。

## 已確認假設
- 既有檔名與編號原則上不改，只搬至正確節次；各節索引預設命名為 **`00_第N節工作索引.md`**。
- 現有 `_work`、`_qa`、`_staging`、`_backup` 依內容歸入第1節或第2節；只有同時涵蓋多節的搬移清冊與回復證據可留在單元根目錄的 **`_history/`**。
- 尚未開始的 `README.md` 只標示節次與「尚未開始」；該節動工時更新其狀態，不虛構內容或進度。

## 已採納建議
- 搬移前後保存完整清冊、檔數、總位元組與逐檔 SHA-256，並保留可回復的搬移證據。
- 同步更新單元／節次索引、Markdown連結、重建腳本與所有現行參照路徑。
- 跨節共用的大型資料只保留一份權威版本，由各節索引連結，不複製造成版本分歧。
- 單元根目錄不得散落任何可判定為單一節次專用的檔案或資料夾。
- 舊的 confirmed RDQ 與歷史對話保留原文；只新增本規格與新規則，不回寫歷史契約。

## 本次不納入
- 不改寫教案或教材內容，不重做已完成成果，不變更正式節數、單元名稱或週次。
- 不整理 202609 四上以外的既有內容；四下、三上、三下先把規則寫入 Skill，日後處理各單元時再依正式來源建立結構。
- 本規格確認與執行不等於 Git commit、push、Obsidian 收工同步或對外發布。

## 一段式需求規格
在 **`G:\我的雲端硬碟\teacher-toolkit\tools\localcoursesErshui\202609 在地課程4年級上學期教案\扇形車庫 第1-2週`** 依正式2節建立 **`01_第1節/`** 與 **`02_第2節/`**，把所有第1節、第2節檔案及其工作、QA、暫存與歷史資料依內容分流，單元根目錄只留下跨節索引、來源追溯／設計決定、2節骨架與真正共用資料；正式成果放各節根目錄，支援資料放 `_work/`、`_qa/`、`_history/`，不得複製大型共用檔；搬移前後以清冊、檔數、位元組與逐檔 SHA-256 驗證，並修正索引、連結、腳本及參照路徑；再將「正式節數等於節次資料夾數、未開始節次放 README、單元根目錄不得散落節次專用資料」寫入專案唯一可編輯的 **`skills/ershui-local-curriculum-builder`** 並用同步工具更新個人安裝副本，且不改寫舊 confirmed RDQ 或歷史對話。

## 驗收條件
- [x] 〈扇形車庫〉存在2個節次資料夾，單元根目錄不再有第1節或第2節專用的散落檔案／資料夾。
- [x] 搬移前後既有檔案全數可追溯，清冊、檔數、位元組與逐檔 SHA-256 相符；例外均有明確紀錄。
- [x] 單元與節次索引、Markdown連結、重建／QA腳本可用，舊現行路徑掃描為0或只剩明確保留的歷史文字。
- [x] Skill正式來源完成規則、驗證與同步，個人安裝副本清冊及 SHA-256 一致；舊 confirmed RDQ 與歷史對話原文未變。

## 執行結果（2026-08-26）
- 〈扇形車庫〉搬移前402檔、1,078,552,307 bytes；移動後、改檔前402/402檔SHA-256一致。最終378檔不變、24檔為索引／路徑／重建及QA紀錄的預期修改，缺檔0；正式PDF與正式頁面PNG未改。
- 四個正式單元的節次資料夾數依序為2、8、4、6；未開始的18個節次均有短README。
- 第1節40分鐘／8表、第2節40分鐘／9表驗證通過；正式14–17號讀回通過，學生與教師PDF各7頁且原SHA-256未變。
- Skill來源與安裝副本各26檔，manifest SHA-256均為 `8E203937690AC024CFBEA0BF1C3B8A1FDDAEB8AFFAAF9880A84031A8455805AA`；3項測試、官方quick validator及實際工作區0 errors／0 warnings均通過。
