import {chromium,expect} from '@playwright/test';import fs from 'node:fs/promises';
const browser=await chromium.launch({channel:'msedge',headless:true}),p=await browser.newPage({viewport:{width:1440,height:1000}});const errors=[];p.on('pageerror',e=>errors.push(e.message));const root='https://gamesinclass-5d9d1.web.app';const res=await p.goto(root+'/?release=20260915-fastload1');await expect(p.locator('#pin')).toBeVisible();await p.screenshot({path:'qa/spin-fastload-production-20260915/login.png'});await p.goto(root+'/join');await expect(p.locator('input').first()).toBeVisible();await p.screenshot({path:'qa/spin-fastload-production-20260915/join.png'});expect(errors).toEqual([]);await fs.writeFile('qa/spin-fastload-production-20260915/browser.json',JSON.stringify({passed:true,loginStatus:res.status(),errors,checks:['Teacher login visible','Student join visible'],limit:'Authenticated gameplay validated locally; production teacher login not performed.'},null,2));await browser.close();console.log('Public production pages passed');
