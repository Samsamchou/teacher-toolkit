# Beach Cleanup／東岸淨灘挑戰：完成與驗證紀錄

日期：2026-09-10。使用者已確認手動「點選物品即可撿拾」並指示開始製作。

## 已完成 / Implemented

- 首頁新增內建 Beach Cleanup 卡片，可在原管理區改名、排序、顯示／隱藏。
- 教師開局選手動／自動，兩組選不同角色，六回合固定角色；問答由教師判定。
- 手動依路徑順序點選發光物品，角色前往撿拾；意外事件自動觸發。自動約 12 秒完成相同事件。
- 每次 A–E 隨機對應 450、500、550、650、700 分；角色不影響機率。兩組各六條、共十二條路徑。
- 七種事件、正負分即時顯示、暫停、分頁失焦暫停、教師調分、提早結束、平手共同勝利與 JSON 成績匯出。
- 左上小型畫筆列：書寫、直線、正方形、矩形、圓形、色彩、局部橡皮擦、復原、全部清除；繪圖時凍結遊戲進度。
- 全螢幕、靜音、減少動態、拾取／意外音效、勝利音效及星星慶祝動畫。
- 教師設定英中雙語，遊戲標籤純英文。沿用全站字級、音量與靜音設定。
- 淨灘課堂以 `gameId: beach` 獨立儲存，可選用共用圖片庫，舊備份與新內建遊戲相容。

## 素材 / Artwork

- `public/beach/east-coast.png`：1 張 gpt-image-2 Images API medium、1536×1024 原創臺灣東岸風格背景。已目視確認並重新計算 SHA-256，與相鄰 JSON 相符。
- SHA-256：`838722005fbb0fc24a7b40f003f5533f363550038012d6c1f56167c8db7fab60`。
- `src/BeachArt.jsx`：五位原創向量角色、七種向量物件；不是 AI 點陣圖。

## 驗證 / Verified

- `npm test`：18 / 18 通過，包含 8 項淨灘／備份測試與既有測試。
- `npm run build`：通過。打包後主要 JS 約 933 KB，仍有超過 800 KB 的非阻擋警告。
- `tests/beach-browser.mjs`：Edge 本機示範環境，1440×900 與 1024×768；觸控點擊、連點、畫筆／橡皮擦／清除、課堂存取、角色互斥、提前結束、暫停與失焦、全局 12 條自動路徑、純英文與成績匯出均通過。
- 自動路徑計時：12014、12014、12014、12000、12000、12012、12000、12000、12015、12000、12000、12015 毫秒。瀏覽器虛擬時鐘驅動實際畫面；不是教室設備實測。
- 該局兩組分數 3300 / 3700，與結果匯出一致；數值每局可不同。
- 額外瀏覽器驗證：全螢幕進出、全螢幕畫筆按鈕、教師 +100 調分及結算動畫渲染。
- 背景與角色、題目頁、手動遊戲頁、平板頁已目視檢查。

## 使用與限制 / Handoff

- 本機預覽：`http://127.0.0.1:5178/`，伺服器位於暫存建置目錄 `C:\Users\User\AppData\Local\Temp\gsg-deploy-20260907`。預覽資料只在該瀏覽器保存。
- 原始碼保留在本專案；暫存目錄用於避開 Google Drive 的執行／EPERM 權限限制。
- 本局進度僅保留在開啟中的遊戲，離開／重新整理會失去本局；結束後可下載結果。課堂圖片清單可另行儲存。
- 新遊戲尚未正式部署，也未 commit／push。依 `agent.md`，正式部署需另行授權。
- 實體 75–86 吋觸控螢幕、教室音量與後排觀看需再試玩新遊戲。
- 六碼登入修復狀態見 `AUTH-FOLLOWUP-20260910.md`：Authentication 設定已存在，但有效通行碼登入／登出仍需教師親自操作，不在本機示範測試範圍。
