# Plink-oh!／歡樂落球 — 本機交付

2026-09-19。狀態：已實作、本機程式與瀏覽器驗證完成；影音相似度待教師驗收；未部署。

## 開啟與重新啟動
- 預覽：http://127.0.0.1:5194/ ，首頁選 Plink-oh!／歡樂落球。
- 音效 A/B：http://127.0.0.1:5194/qa/plinkoh-20260919/sound-check.html 。8 組原片／候選音，可標記可接受／需修改並匯出。
- 驗收頁：http://127.0.0.1:5194/qa/plinkoh-20260919/index.html 。含錄影、截圖及來源對照。
- 重新啟動：在本專案 PowerShell 執行 `& '.\scripts\plinkoh-preview.ps1'`。
- Drive 是來源；執行副本在 `%LOCALAPPDATA%\Temp\gsg-plinkoh-20260919`。此電腦沿用既有本機依賴；別台電腦第一次會執行 npm ci。

## 已完成
- 內建 plinkoh 入口，初始化、登入後清單、顯示／隱藏、編輯保護及 ZIP 備份識別一致整合。
- 管理與設定頁英中雙語，上課遊戲純英文；完全沒有題目、答案或判答依賴。
- 2–15 組、可改隊名、預設6回合（設定1–12）、5／10上方放球區、自由選組、每組每回合一次。
- 真實固定步長落球、碰柱消失與加分、底部反彈、落定單次結算、分數飛向當前組、皇冠及回合進度。
- 教師確認後可調分、略過、結束、重開；JSON 分數紀錄可匯出。分数顯示於移轉動畫完成後更新，保存則在結算時原子更新。
- 設定、分數、已玩組及進度保存在同一瀏覽器。落球中刷新會退回同一組放球前；已結算的分數不會重複入帳。
- 音樂／音效獨立音量、總靜音、平台總靜音、首次手勢啟用、載入失敗重試／無聲繼續、暫停與退出清理。
- 第4–6回合、終局及缺資料規則以差異清單記錄；未確認特殊功能不啟用。

## 核心檔案
- `src/PlinkOhGame.jsx`、`src/plinkoh.css`：介面與向量美術。
- `src/plinkoh-model.mjs`、`src/plinkoh-physics.mjs`：回合、保存與物理。
- `src/PlinkOhAudio.js`、`public/plinkoh/audio/`：8個候選WAV與來源／雜湊清單。
- `tests/plinkoh*`：單元、瀏覽器、觸控／音訊失敗回歸及錄影。
- `rdq/RDQ-spec-plinkoh-20260919.md`：已確認需求。
- `qa/plinkoh-20260919/`：15張參考幀、16種布局截圖、試聽、紀錄及影音證據。

## 驗證依據
- 最終 `npm test`：83／83 通過，exit 0；`npm run build`：exit 0。
- `verification.json` 與 `test-output.txt`、`build-output.txt` 記錄最終命令及退出碼。
- `browser-report.json`：12次實際落球完成6回合，16個組數／尺寸組合，防連點、刷新恢復、暫停、調分、首頁保存及5／10區切換，無頁面錯誤。
- `edge-report.json`：觸控、音效故障重試、音訊清理、內建編輯保護、四款既有遊戲入口、/join及/unscramble入口，無頁面錯誤。
- `recording-report.json`、`gameplay-with-audio.mp4`：實際頁面畫面＋遊戲音訊；量測非靜音。
- 物理壓力測試600次落球正常落定，30／60／144 FPS一致；15組六回合90次結算模型測試通過。
- 未重新驗證正式站有效通行碼登入、多裝置同步、實體iPad／觸控大螢幕、教室揚聲器。沒有修改後端或正式站。

## 教師尚需驗收
1. 音效A/B的音色、尾音、混音殘留與背景音樂循環；8項均保留 pending，不宣稱原音復刻完成。
2. 與原片的畫面、動作節奏及聲畫同步；已知差異見 `reference-gap.md`。
3. 真實課堂設備的觸控、投影可讀性與音量。瀏覽器模擬觸控不能代替實機。

## 保存界線
本機進度不包含在既有圖片庫ZIP中；可另從本遊戲選單匯出JSON紀錄。清除網站資料、更換瀏覽器或網址來源會失去本機接續；沒有雲端接續，也沒有匯入歷次JSON重開局功能。

## 狀態與保留
原始資料夾及影片保留。既有 Unscramble 的15組、重登及移除登入修改保留。`public/release.json` 是新的 local-preview-not-deployed 清單；沒有正式部署、Git提交或推送。本機production build仍有既有整合bundle超過800KB的非阻斷提醒。
