# 糖果樂園改版驗證 / Candy redesign QA

2026-09-05。本次只修改本機來源與示範頁，沒有部署 Firebase 或改動正式後端。

## 已完成
- 首頁、登入、圖片庫、課堂、遊戲入口、備份與提示以英中雙語呈現。教師自行命名的圖片／課堂內容保留原文。
- 英文主要按鈕 28px 起、中文 22px 起；提供大字／加大／最大。檢查圖片庫無小於 22px 的可見文字；115% 時上傳區英文 29.9px、中文 25.3px。
- 四款透明 PNG 由內建 image_gen 產生並視覺檢查，均 1254×1254，角落 alpha=0；每款附來源提示與 SHA-256 記錄。未使用 Images API。
- Chrome 真實操作：手動刮圖、直橫圖切換、矩形註記、縮小視窗後筆跡相對位置保留。
- 揭曉完成記錄 8010ms，接四角色同時出現；慶祝結束記錄 6006ms。
- 低動態模式中四角色 animationName 均為 none；Skip celebration 立即將慶祝狀態變為 false。
- 管理設定雙語；遊戲主畫面、畫筆與音效設定均純英文。
- 音效以本機 Web Audio 合成刮擦與勝利旋律，沒有外部音檔、即時 AI 或音樂授權依賴。停止移動 100ms 淡出，放開／離開立即停止；靜音與分別音量設定保存於本瀏覽器。
- 完整手動刮除遮罩時也觸發慶祝；不把局部刮圖當成完整揭曉。
- 瀏覽器執行過刮擦、勝利試聽操作，未出現音效 JavaScript 錯誤。實際喇叭音色不以程式測試代替教師試聽。
- 既有 16 張圖片（含使用者新增照片）與原有課堂保留。新增 Candy celebration check 測試課堂只使用幾何示範圖。

## 自動驗證
- npm run build：正式建置通過；JS bundle 約 899 KB 的大小提示仍存在。
- npm test：7 項既有模型測試＋3 項音效生命週期測試，最後執行 10/10 PASS。
- 權限規則本次沒有變更，沿用 QA-20260905.md 的模擬器結果，未冒充重新部署或正式驗證。

## 尚需教師實機確認
75–86 吋觸控螢幕的手指操作、全螢幕、最後一排可讀性，以及實際喇叭刮擦音色與勝利音量。正式通行碼、跨裝置同步仍待 Firebase 部署後驗收。
