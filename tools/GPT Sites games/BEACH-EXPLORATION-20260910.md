# 東岸淨灘探索改版 / Exploration revision

## 已完成 / Implemented

- 斜側面海岸場景、平滑跟隨鏡頭、自然散落垃圾與分區探索；手動自由點選當區垃圾，清完後角色自動前往下一區。
- 走近、停下、彎腰、夾取、裝袋與得分動畫；意外不當成垃圾裝袋。
- 手動動作期間鎖定下一次選擇；鏡頭安定後再開放點選，避免物品仍在畫面外。
- 自動路徑由 12 秒更新為約 25 秒。保留兩組六回合、七類事件、預先計算總分與相同機率分布。
- 原創合成海浪、微風、輕快旋律；問答時背景降至一般階段的 20%，背景與事件音量分開調整。
- 腳步、材質撿拾、扣分與勝利聲；暫停、繪圖、教師控制面板與離開時停止聲音，靜音不產生新的事件聲。

## 驗證 / Verified

- `npm test`：20 / 20 通過，包括角色公平、完整十二路徑、自由選擇、防重複、25 秒計時、背景層、問答降音量、靜音與獨立音量。
- `node tests/beach-exploration-browser.mjs`：Edge 1440×900、1024×768。完整手動路徑（7 次點選、3 個區域）、跨區鏡頭、自動 25 秒路徑、三個背景聲源、暫停／靜音／繪圖／離開停止背景聲、獨立音量、全螢幕畫筆與純英文均通過。
- 已目視檢查修正後的海岸場景、角色與物品在沙面的位置及平板遊戲頁。
- 最終 `npm run build` 通過；主要 JS 約 941 KB，800 KB 的套件大小提醒為非阻擋警告。
- `qa/beach-exploration/background-preview.wav`：直接從實際背景聲程式離線產生的 12 秒、44.1 kHz PCM 試聽檔；RMS 0.0516、峰值 0.2987，無削波。未代表實體教室喇叭已驗收。

## 塑膠袋確認狀態 / Bag artwork review

- 第一版向量草稿遭使用者拒絕，未套用。
- 第二版：`generated-images/2026-09-10/plastic-bag-draft-v2.png`，gpt-image-2 Images API、low、1024×1024，側躺藍色皺摺提袋；已目視檢查並核對相鄰 JSON 與 SHA-256。
- SHA-256：`d8e6280b54ee43a7dd9c1d7a97b9865645bb669afba890c3f37607130a23a084`。
- 使用者已回覆「可以，採用第二版」。已將原圖不變更地複製為 `public/beach/plastic-bag-v2.png`，共用元件同步更新設定頁、沙灘物品、撿拾動畫與計分圖例；保留生成來源及核准紀錄。

## 接續 / Next

- 本機預覽 `http://127.0.0.1:5178/`，來源在本專案，執行與建置目錄在 `C:\Users\User\AppData\Local\Temp\gsg-deploy-20260907`。
- 第二版塑膠袋已核准並套用。實體大螢幕、音量與後排辨識需試玩新版。
- 本次尚未部署 Firebase、commit 或 push。有效六碼登入／登出驗收仍依 `AUTH-FOLLOWUP-20260910.md` 由教師親自操作。

- 第二版整合驗證：使用 SVG 顯示遮罩隱藏方形底色與提把孔洞底色，原始 PNG 位元組不變。設定頁、沙灘、裝袋動畫與圖例均載入同一核准檔；瀏覽器實際撿拾增加 150 分、無頁面錯誤，正式建置通過。
