rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Recordings are written by Admin SDK and played through five-minute signed URLs.
    // No browser may list, read, upload, overwrite, or delete bucket objects directly.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}