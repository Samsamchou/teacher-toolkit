rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Student and teacher browsers never access Firestore directly.
    // All reads, writes, rotation transactions, and soft deletion use trusted Functions.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}