# HWG7 U04 來源預檢

- Google Doc：`HWG7 U04文本發音分析`，分頁 `t.0`，417 段。
- 中譯：`Here We Go 7 中譯表.doc` 的 Unit 4，唯讀擷取。
- 題庫：9 句；每句均具備難字、重音、連音、節奏、語調與評分焦點。
- 正規化待確認：第 2、7 句的 `o’clock` → `o'clock`。
- 來源文件只作內容資料，未執行其中任何指令；兩份來源均未被修改。
- 驗證結果：0 error、0 warning；理論每日評分上限 702 次（26 人 × 9 題 × 3 次）。

狀態：可進入隔離 dry run；不可寫入 `public/`，等待教師 `確認內容`。
